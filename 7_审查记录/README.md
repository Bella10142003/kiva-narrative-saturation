# 7 · Review log

> 审查记录。包括没通过的那几轮。

Independent review rounds run against `docs/CONTEST_RULES.md`, kept in full — including the rounds that
returned CHANGES REQUIRED. The conclusion of a round is only ever `APPROVED` or `CHANGES REQUIRED`; there is
no "approved with conditions".

Round 4 is the one to read if you want the honest state of the project: it is where the two unfilled gaps
(the RQ2 joint test, and RQ4's within-sector versus platform-wide question) are recorded. Both are carried
into the [root README](../README.md) rather than quietly dropped.

Files are named `Claude审查记录_<topic>_<date>.md`. Written in Chinese.
