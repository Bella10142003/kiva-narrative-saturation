# Claude 独立审批记录 · 最终交付包（第二轮）

> 本文件是 `Claude审查记录.md`（2026-08-15，提案阶段）之后的独立一轮，审查对象为最终交付包与分析代码，编号从上一轮续接（Major 续 M-09，Minor 续 m-13）。

## 审查信息

| 项目 | 内容 |
| --- | --- |
| 审查日期 | 2026-08-30 |
| 审查对象 | `5_最终交付包/MA-Hackathon-Final-2026-09-04/`（REPORT.md、JUDGE_QA_BILINGUAL.md、SPEAKER_NOTES_EN.md、outputs/、audit/）与 `3_分析步骤/` 全部 18 个 notebook |
| **审查范围（重要限制）** | **本轮为定向代码与交付物核查，不是 CLAUDE.md 所要求的全量审批。**未逐项复核 REQ-CON-001～010、REQ-DATA-001～002、SCORE-001～005，未重新核验官方模板、字数与排版。**因此不得将本文件中未出现的项目理解为“已核查且通过”。** |
| 已执行的核查 | ①`3_分析步骤/02`、`03`、`04`、`05`、`06`、`07` 逐格代码走查；②`4_中间产物/我算出来的结果/` 与交付包 `outputs/` 数字比对；③在 `core.parquet`、`text_private.parquet` 上独立重跑验证查询（见附录）；④对 REPORT.md／JUDGE_QA／SPEAKER_NOTES 做关键词检索确认披露情况 |
| 执行方式 | 前台单智能体；未启动 Workflow、subagent、多智能体或后台任务 |
| 是否修改起草文件 | 否。按 CLAUDE.md，仅写入本审查记录，未改动 `2_提案/`、`3_分析步骤/`、`5_最终交付包/` 任何文件 |

## Blocker

本轮未发现新的 Blocker。上一轮 SB-01／SB-02／SB-03 的状态未在本轮复核，仍以 `Claude审查记录.md` 为准。

## Major

| 编号 | 位置 | 对应标准 | 原因与影响 | 修改建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| M-09 | `5_最终交付包/MA-Hackathon-Final-2026-09-04/REPORT.md:20-26`「研究范围与数据边界」 | SCORE-002（严谨性）；SCORE-005（清晰度）；CONTEST_RULES 可追溯性 | 该节只披露了「6 条 `raisedDate < fundraisingDate` 被排除」，未披露建模样本的其余三步损耗：wash-in −19,645、`use` 文本为空 −37,537、池阈值 `n>=10` −29,118，合计从 1,453,840 降至 1,367,540（−5.94%）。池阈值这一步总量仅 2.08%，但**按行业高度非随机**：Entertainment 100%、Water 99.86%、Wholesale 98.18%、Manufacturing 46.64% 的贷款不达标。后果是 `rq4_sector_effects.csv` 只覆盖 **14 个行业**，而 `sector_counts.csv` 有 **19 个**，缺失 Manufacturing、Sanitation & Hygiene、Water、Wholesale、Entertainment 五个。报告正文、JUDGE_QA 与 SPEAKER_NOTES 中 `Entertainment`、`Wholesale`、`n>=10`、`pool_size_by_sector` 均**零次出现**；HTML 报告局限性仅有一句未量化的「只覆盖可估计子集」。证据表已在交付包内，但正文未引导读者查看——评委把两张表并排即可发现 5 个行业消失，届时属于被动暴露而非主动披露 | 在「研究范围与数据边界」补一条四步损耗链，并明确写：H4 的行业异质性结论覆盖 14 个可估计行业；Manufacturing、Sanitation & Hygiene、Water、Wholesale、Entertainment 因活跃池规模过小（Entertainment 活跃池中位数为 1）无法估计叙事饱和度，已排除。来源标 `outputs/model_sample_flow.csv`、`outputs/pool_size_by_sector.csv`。**不需要重跑任何分析**，两张证据表已存在且数字自洽 | 未解决 |
| M-10 | `2_提案/提案正文.md:52` 的承诺 vs 交付包全体 | REQ-CON-001；SCORE-002；**上一轮 M-07 的遗留** | 上一轮 M-07 的获批修改方式，是在提案中加入承诺：「Where *disbursalDate* precedes posting, faster funding refreshes partner capital rather than bringing the borrower's cash forward; **we will audit how often this holds and control for the gap.**」该承诺含两项义务：①审计发生频率；②在模型中控制 gap。本轮核查结果：`disbursal_ts` 在 `02` 第 6 段算出并写入 `core.parquet` 后，**在 `03`–`17` 全部 notebook 中再未被引用**；`07` 的 `CORE_CONTROLS` 无 gap 变量；交付包 `outputs/` 无任何预放款审计表，`disbursalDate` 仅出现在 `raw_schema.csv` 与 `column_null_rates.csv`；REPORT.md 五条局限性均未提及。独立重跑显示该情形并非边缘案例而是**样本主体：96.36%**（1,400,955／1,453,840）的贷款 `disbursalDate` 早于 `fundraisingDate`，gap 中位数 24.94 天；且与结果变量非正交（预放款组中位筹款 67.28 小时、72 小时达成率 51.02%，非预放款组 107.55 小时、43.87%）。这意味着提案自己提出的告诫适用于近乎全部样本：对 96.4% 的贷款而言，「筹款更快」补充的是合作机构的周转资金，并未让借款人更早拿到钱。报告的管理含义虽已转向 platform/partner 流程（`REPORT.md:52`），但从未披露这一比例，也未兑现已获批的承诺 | 二选一：**（最小修复，不需重跑模型）** 用 `core.parquet` 生成一张 `outputs/predisbursal_audit.csv`（预放款占比、gap 分位数、分组结果对比），在「研究范围与数据边界」和局限性各补一句，说明 96.4% 为预放款、故 funding speed 的受益方主要是 partner 周转而非借款人现金到账时点。**（完整修复，需重跑 07）** 在此基础上把 `log(1 + gap_days)` 加入 `CORE_CONTROLS`，兑现「control for the gap」。gap 由上架前信息构成，不属于 look-ahead，可安全入模 | 未解决 |

## Minor

| 编号 | 位置 | 对应标准 | 原因与影响 | 修改建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| m-13 | `5_最终交付包/MA-Hackathon-Final-2026-09-04/outputs/text_selection_diagnostics.csv` 的 `representation_note` 字段；同源于 `src/freeze_spec.py` 与 `notebooks/S1_audit.ipynb` | SCORE-005（清晰度） | 该字段写作「deterministic 5% training sample; **hashed unigram+bigram TF-IDF** for field selection only」，但产生该表的 `diagnostic_overlap()` 只使用了一个 `HashingVectorizer(norm="l2")`，**全程无 IDF 环节**（IDF 要到 Stage 2 才由 `04` 在训练期拟合）。实际表示是 hashed TF + L2 归一化。此偏差不影响字段选择结论——`use` 与 `description` 使用同一套表示，相对比较仍然公平——但措辞与代码不符，评委对照 `src/freeze_spec.py` 即可发现。**注意：`frozen_spec.csv` 中「Masked hashed unigram+bigram TF-IDF」一句描述的是 Stage 2 正式管线，那里确有 train-only IDF，用词正确，不在本条范围内** | 将 `representation_note` 改为 `hashed unigram+bigram term frequency (L2-normalised, no IDF) for field selection only`。需重跑 `03`（约 25 秒）并同步更新交付包内同名文件；不影响任何数值 | 未解决 |

## 附录 · 本轮独立验证的数字

以下数字由 Claude 在 `4_中间产物/data/clean/core.parquet` 与 `data/private/text_private.parquet` 上独立重跑得到，可复算。

**A. 建模样本损耗链**（与 `outputs/model_sample_flow.csv` 一致）

| 步骤 | 剩余 | 本步扣除 |
| --- | --- | --- |
| 有效时长 | 1,453,840 | — |
| wash-in eligible | 1,434,195 | −19,645 |
| raw text nonempty | 1,396,658 | −37,537 |
| 池阈值 `n>=10` | 1,367,540 | −29,118（2.08%） |

**B. wash-in 扣除的构成**（解释 `pool_size_precheck.csv` 中 lag 池 `pct_below_10 = 0.879%` 的来源）

| 时段 | 焦点贷款 | `pool_n = 0` | `pool_n < 10` |
| --- | --- | --- | --- |
| 2016 前 35 天（lag 窗口全空） | 9,832 | 9,832 | 9,832 |
| 2016 第 35–65 天（窗口残缺） | 9,813 | 56 | 385 |
| 第 65 天之后（窗口完整） | 1,434,195 | 421 | 2,562 |

合计 `pool_n < 10` = 12,779／1,453,840 = 0.879%，与 CSV 精确一致。剔除前 65 天后该比例降至 **0.179%**，低于 `posting_14d` 的 0.245%——即 lag 池的高不达标率完全由数据边界效应造成，非池子本身不足。两段合计 9,832 + 9,813 = 19,645，与 A 表 wash-in 扣除数一致。

**C. 预放款（M-10 依据）**

| 指标 | 数值 |
| --- | --- |
| `disbursal_ts` 缺失 | 0 |
| `disbursal_ts < fundraising_ts` | 1,400,955（**96.36%**） |
| gap 天数 p10／p50／p90 | 6.35／24.94／42.19 |
| 预放款组：中位筹款小时／72h 达成率 | 67.28／51.02% |
| 非预放款组（52,885 笔）：同上 | 107.55／43.87% |

**D. 行业池覆盖**（引自 `outputs/pool_size_by_sector.csv`，未修改）

| 行业 | 贷款数 | 活跃池中位数 | 不达标占比 | 是否进入 `rq4_sector_effects.csv` |
| --- | --- | --- | --- | --- |
| Entertainment | 535 | 1 | 100.00% | 否 |
| Water | 1,466 | 2 | 99.86% | 否 |
| Wholesale | 989 | 3 | 98.18% | 否 |
| Manufacturing | 9,024 | 10 | 46.64% | 否 |
| Sanitation & Hygiene | 6,905 | 131 | 25.46% | 否 |
| Clean Energy | 35,342 | 17 | 33.43% | 是 |
| Arts | 18,328 | 30 | 18.85% | 是 |

**E. 文本字段的 HTML 标记**（支持 m-13 的背景核查，非独立问题）

`description` 有 1,151,345 行（79.2%）含 HTML 标签，以 `<br />` 为主；`use` 仅 2 行。两字段 HTML 实体均为 0，即 `html.unescape()` 在本数据集上为空转。`TAG_RE` 去标签对 `description` 必要——若不去除，`br` 会成为高频 token，而 Stage 1b 的诊断无 IDF 压制，会系统性抬高 description 的余弦相似度，进而影响字段选择判据。当前代码已正确处理，此处仅作记录。

## 未在本轮核查的范围

以下项目本轮**未做检查**，其状态不因本文件而改变：

- REQ-CON-001～010、REQ-DATA-001～002、SCORE-001～005 的逐项核查
- 官方模板、文件命名、字数、排版、引用格式与提交渠道
- `2_提案/requirements.md`、`compliance-matrix.md`、`evidence.md`、`assumptions.md` 的同步状态
- 上一轮 SB-01／SB-02／SB-03 与全部未解决 Minor（m-01～m-12）
- PPTX、HTML 报告的渲染核查
- `08`–`17` notebook 的逐格代码走查

## 审批结论

<!-- 下一行必须且只能填写 APPROVED 或 CHANGES REQUIRED -->

CHANGES REQUIRED
