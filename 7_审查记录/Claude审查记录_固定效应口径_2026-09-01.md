# Claude 独立审批记录 · 固定效应口径（第三轮）

> 本文件续接 `Claude审查记录.md`（2026-08-15，提案阶段）与 `Claude审查记录_最终交付包_2026-08-30.md`（第二轮）。编号从上一轮续接（Major 续 M-11）。

> ## ⚠️ 本轮处置已被第四轮修正 —— 文内数字均为历史值，非现值
>
> 本轮把 `FIXED_EFFECTS` 由七重改为三重，代价是 `gender`／`repaymentInterval`／`posting_dow`／`posting_hour` **被完全丢出模型**，违反提案 PDF p.4 对 "posting-time characteristics" 的承诺。第四轮 **M-12** 已修正为：**三重固定效应保持不变，四项改入 `CORE_CONTROLS`**。
>
> | 项 | 本轮记录的值（已过期） | **当前值** |
> | --- | ---: | ---: |
> | 头条 C&H 情景 | +123.51% | **+124.35%** |
> | β(C) | 0.5131 | **0.5202** |
> | β(H) | 0.4137 | **0.4036** |
> | β(C×H) | 0.1045 | **0.1017** |
>
> 引用数字或据以改动代码前，请以 `Claude审查记录_提案基准更正_2026-09-01.md`（第四轮）为准。

## 审查信息

| 项目 | 内容 |
| --- | --- |
| 审查日期 | 2026-09-01 |
| 审查对象 | `5_最终交付包/MA-Hackathon-Final-2026-09-04/src/fit_main_models.py` 及其下游全部产物 |
| **审查范围（重要限制）** | **本轮为单点定向核查，起因是用户询问 `06_准备建模矩阵.ipynb` 的控制变量逻辑，核查过程中发现固定效应口径不一致。不是 CLAUDE.md 所要求的全量审批。** 未复核 REQ-CON-001～010、REQ-DATA-001～002、SCORE-001～005，未复核第二轮遗留的 M-09／M-10／m-13。**不得将本文件中未出现的项目理解为「已核查且通过」。** |
| 已执行的核查 | ①比对 `freeze_spec.py`、`frozen_spec.csv`、`README.md` 与 `audit/stage4b_model_manifest.json` 中记录的实际估计公式；②在隔离环境中以三重固定效应重跑 `fit_main_models.py`，逐系数比对；③全链路重跑与交付包重建 |
| **是否修改起草文件** | **是。** 用户在本轮中明确指示「改回三重重跑」并选择「全链路：代码+产物+交付包」，构成 CLAUDE.md 所要求的明确授权。修改清单见下文「处置记录」 |
| 执行方式 | 前台单智能体；未启动 Workflow、subagent、多智能体 |

## Blocker

本轮未发现新的 Blocker。

## Major

| 编号 | 位置 | 对应标准 | 原因与影响 | 修改建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| M-11 | `src/fit_main_models.py:26-34` 的 `FIXED_EFFECTS` 常量，对照 `src/freeze_spec.py:357`、`outputs/frozen_spec.csv`「Model and claim」行、`README.md` 步骤表第 07 行 | CONTEST_RULES 可追溯性；SCORE-002（严谨性）；**冻结机制本身的有效性** | `freeze_spec.py:357` 冻结的规范是 `"HDFE with Country + Activity + Week FE; country/week clustered SE"`，即**三重**固定效应；`frozen_spec.csv` 与 `README.md` 亦均写「国家+活动+周三重固定效应」。但 `audit/stage4b_model_manifest.json` 记录的实际估计公式为 `\| country_name + activity + week_id + gender + repaymentInterval + posting_dow + posting_hour`，即**七重**——多出 `gender`(2 类)、`repaymentInterval`(3 类)、`posting_dow`(7 类)、`posting_hour`(24 类)。`METHODS_APPENDIX.md:32` 与 PPTX 幻灯片 13 同样按七重描述，与 `frozen_spec.csv` 直接冲突。<br><br>**方向判断**：多出的四组均为低基数，加入后模型更饱和，通常使系数更保守而非更有利，不像事后调参。**但这正是冻结机制要防的那类事**——`03_冻结变量定义` 存在的全部意义就是「看结果前写死、跑完后文档与实际必须对得上」。`frozen_spec.csv` 随交付包提交，评委对照 `stage4b_model_manifest.json` 即可发现三份文档说三重、代码跑七重。属被动暴露 | 二选一：**（A）** 将 `FIXED_EFFECTS` 改回三重并重跑全链路，使实际执行回到冻结口径；**（B）** 保留七重，但需在 `freeze_spec.py`、`frozen_spec.csv`、`README.md`、`METHODS_APPENDIX.md` 中一并更新并说明理由——惟此做法是在看到结果之后修改冻结文档，与冻结机制的初衷相悖。<br>**推荐 A**：冻结文档是承诺，代码是对承诺的执行，偏离的一方应当是代码 | **已解决，但方案已被第四轮修正** —— 本轮按方案 A 执行后，四个变量被完全丢出模型，违反了提案 PDF p.4「posting-time characteristics」的承诺（第四轮 **M-12**）。最终方案为：`FIXED_EFFECTS` 保持三重不变，四项改入 `CORE_CONTROLS`，两份文档同时满足。**本行的方案 A／B 二选一已不适用，勿据此改动代码**，以 `Claude审查记录_提案基准更正_2026-09-01.md` 的 M-12 为准 |

## 处置记录

用户指示：改回三重固定效应并重跑；范围选择「全链路：代码+产物+交付包」。

### 一、修改前的验证（隔离环境）

在 scratchpad 中以符号链接接入 `model_data.parquet`，仅改 `FIXED_EFFECTS` 为三重后重跑，**不触碰任何项目文件**，先确认改动是否安全。结论：`rq1_main_table`、`rq2_raw_vs_residual`、`robustness`、`scenario_contrasts` 四表共 44 项，**符号翻转 0 项、显著性翻转 0 项**。确认安全后才开始实际修改。

### 二、代码修改

| 文件 | 修改内容 | 副本数 |
| --- | --- | --- |
| `src/fit_main_models.py` | `FIXED_EFFECTS` 七重 → 三重 | 3（交付包、`_rebuild`、`_rebuild/_verify`） |
| `src/build_deliverables.py` | `METHODS_APPENDIX.md` 中写死的模型公式去掉四项 | 3 |
| `src/build_presentation.mjs` | 幻灯片 13「CONTROLS + FE」块删去 `gender · repayment · day · hour` 行 | 3 + `3_分析步骤/附_生成PPTX.mjs` |
| `src/final_qa.py` | 头条数值回归锁更新，并按代码既有体例补注变更理由 | 3 |
| `3_分析步骤/07_主模型HDFE.ipynb` | cell 1 内嵌的 `FIXED_EFFECTS` 同步为三重 | 1 |
| `Kiva_Final_Presentation_2026-09-04.pptx` | node 不可用无法由 `.mjs` 重生成，改为直接移除 `ppt/slides/slide13.xml` 中对应的 `<a:p>` 段落 | 3 |

`fit_heterogeneity.py`(08) 与 `description_robustness.py`(10) 均 `from fit_main_models import FIXED_EFFECTS`，自动跟随，无需单独修改。

`predict_engine.py` **未修改**：其中的 `posting_dow`／`posting_hour` 是 Engine 预测模型的类别特征，不是 HDFE 的固定效应；冻结规范约束的是 HDFE 因果模型，不涉及 Engine 的特征集。

`5_最终交付包/_control/` **未修改**：它是原始交付包的完整快照（`outputs/` 逐文件 MD5 与原始交付包一致，`final_qa.py` 回归锁 124.38126636771179 亦与原始交付包一致）。修改后已从备份完整还原，现作为「改动前」对照。

> 更正：核查过程中一度将 `_control` 的锁值 124.3813 误判为「更早版本」。实际相反——124.3813 是**原始交付包**的值；是提升 `_rebuild` 时交付包的 `final_qa.py` 才变成 `_rebuild` 血缘的 124.3508，因而与 `_control` 出现差异。还原 `_control` 的处置本身仍然正确。

### 三、重跑链路

按 `RUNBOOK.md` 的顺序，在 `_rebuild/MA-Hackathon-Final-2026-09-04/`（带 `data/` 的完整构建目录）执行：

`fit_main_models` → `fit_heterogeneity` → `description_robustness` → `check_joint_support` → `make_figures` → `create_and_execute_notebooks` → `build_deliverables`，随后提升至交付包并执行 `final_qa` 与 `build_final_zip`。`4_中间产物/我算出来的结果/` 亦同步重跑。

`predict_engine` 未重跑（不受固定效应影响，重跑只会引入无谓的浮点漂移）。

**确定性门全部通过**：`identity_passed=true`、`joint_support_pass=true`、七个交付包 notebook 全部执行成功。

### 四、结果变动

头条情景对比（`outputs/scenario_contrasts.csv`）：

| 情景 | 七重（改前） | 三重（改后） |
| --- | ---: | ---: |
| C&H 从 P25→P75，筹款时长 | +124.381% | **+123.506%** |
| ↳ 95% CI | [103.739, 147.115] | [100.867, 148.697] |
| C&H，72 小时快速筹款率 | −14.684pp | **−14.569pp** |
| V&G 通道（本就不显著） | −1.426% | −0.955% |

主表（`outputs/rq1_main_table.csv`，`active_raw_main`）：

| 项 | 七重 | 三重 | 变动 |
| --- | ---: | ---: | ---: |
| C | 0.5203 | 0.5131 | −1.4% |
| H | 0.4036 | 0.4137 | +2.5% |
| C×H | 0.1016 | 0.1045 | +2.8% |
| V | 0.0496 | 0.0651 | +31.1%（p=0.485→0.374，两侧均不显著） |
| G | −0.1385 | −0.1672 | −20.7%（p=0.022→0.008） |
| V×G | 0.0418 | 0.0465 | +11.4% |

R² 由 0.5621 降至 0.5414，within-R² 由 0.3729 降至 0.3578，与少四组固定效应的预期一致。

**四表 44 项：符号翻转 0，显著性翻转 0。** H 的系数上升 2.5%，方向上属「对假设更有利」一侧，但幅度不构成叙事变化，且系恢复冻结口径的必然结果而非择优选取。

### 五、重跑过程中自查发现并修复的次生问题

`build_deliverables.py` 只重生成 `REPORT.md`、`METHODS_APPENDIX.md`、`RUNBOOK.md`、`DELIVERY_MAP.md`、`environment.txt`、`report_artifact.json`。以下四份文档**不在其生成范围内**，重跑后仍停留在旧数字（124.38 等）。改动前它们与当时的 `scenario_contrasts.csv`（124.3813）是一致的，**该不一致由本次重跑引入**，已全部修复：

| 文档 | 处理方式 | 修改处数 |
| --- | --- | --- |
| `JUDGE_QA_BILINGUAL.md` | 手工更新（含标题中的数字） | 3 |
| `SPEAKER_NOTES_EN.md` | 手工更新（含 CI） | 2 |
| `SPEAKER_CUES_ZH.md` | 手工更新（含 CI） | 3 |
| `Kiva_Final_Report_2026-09-04.html` | 见下 | 54 |

`Kiva_Final_Report_2026-09-04.html` 由仓库外的 artifact 渲染工具从 `report_artifact.json` 生成（该 JSON 已由 `build_deliverables.py` 更新为新值），**本地无重生成路径**。采取结构锚定的文本替换：仅在 `<span class="portable-source-value-text">`、`>Source for …`、`<text>…</text>`、`<td class="portable-table-number">`、`<strong>` 五类文本节点内替换，避免 `1.43`、`0.51`、`2.84` 等短串误伤坐标或其他数值。共替换 12 个数值合计 54 处，替换数与事前扫描逐项吻合；替换后字节数（648,329）与标签总数（3,939）均未变化，锚定位置无旧值残留。该 SVG 区块经检查只含 `<text>`／`<g>`／`<tspan>`，无 `rect`／`path`，属文本型指标块，不存在几何编码数值的问题。

复核结果：`REPORT.md`、`JUDGE_QA_BILINGUAL.md`、`SPEAKER_NOTES_EN.md`、`SPEAKER_CUES_ZH.md`、`Kiva_Final_Report_2026-09-04.html` 五份文档现均只含新值（123.51），旧值（124.38）零残留。

### 六、口径一致性复核

| 来源 | 内容 |
| --- | --- |
| `outputs/frozen_spec.csv` | `HDFE with Country + Activity + Week FE` |
| `audit/stage4b_model_manifest.json`（实际估计） | `country_name + activity + week_id` |
| `METHODS_APPENDIX.md:49` | `\| country + activity + week` |
| `README.md` 步骤表 07 | 国家+活动+周三重固定效应 |
| PPTX 幻灯片 03 | `Country + activity + week fixed effects` |
| PPTX 幻灯片 13 | `Country · activity · week`（多余行已删） |

**六处已全部对齐。**

## 遗留事项

| 编号 | 内容 | 影响 | 建议 |
| --- | --- | --- | --- |
| R-01 | `final_qa.py` 20 道门中「PPTX has 16 slides, 16 notes, eight embedded figures and renders 16 pages」一门 **FAIL**，当前为 19/20 | 该门的六个子条件中，slides=16、notes=16、notes_with_sources=15、media=8、zip 无损坏 **五项均通过**，仅 `rendered==16` 不满足——它要求 `audit/pptx_render/slide-*.png` 共 16 张，而该目录**在本工作副本中从未存在**（全项目搜索无任何 `slide-*.png`）。渲染 PPTX 需要 node，本机 `node not found`。<br><br>**已验证为既有问题，非本轮引入**：将改动前的交付包原样复制到隔离目录，用**它自己的** `final_qa.py` 跑**它自己的**产物，结果同为 `FAIL 19/20`，失败的也是同一道 PPTX 门。即交付包内原本记录的 `PASS 20/20` 是原始构建环境（有 node、有渲染产物）留下的记录，与该副本的实际可复算状态本就不符。本轮重跑只是把这一事实如实写了出来 | 在有 node 的环境中渲染 PPTX 生成 `audit/pptx_render/slide-*.png`，再重跑 `final_qa.py`，即可恢复 20/20。**在此之前交付包内的 `FINAL_QA.md` 会显示 FAIL，提交前须处理** |
| R-03 | `Kiva_Final_Report_2026-09-04.html` 系手工文本替换而非工具重生成 | 数值与结构均已复核无误（见处置记录第五节），但严格意义上它不再是由 `report_artifact.json` 渲染出的产物 | 在具备 artifact 渲染工具的环境中从已更新的 `report_artifact.json` 重新生成一次，以恢复完整的生成血缘 |
| R-02 | 流水线非逐位可复现 | 三份 `model_data.parquet`（交付包血缘 188,010,865 / `_rebuild` 188,132,614 / `4_中间产物` 187,961,314 字节）行数同为 1,453,840，但字节数各异，系 ZSTD 并行压缩非确定性；系数在第 5 位小数存在漂移（如 C=0.5202958 vs 0.5202465）。交付包原血缘对应的 parquet 已不存在，**任何重生成都必然带有此漂移**。这是既有属性，非本轮引入 | 若需逐位复现，`prepare_model_data.py` 中 DuckDB 的 `SET threads=8` 与 ZSTD 并行压缩需改为单线程；代价是 06 的运行时间。是否值得由团队决定 |

## 备份

改动前完整备份（`5_最终交付包/`、`3_分析步骤/`、`4_中间产物/` 的 outputs 与 audit，共 376 MB）已保存至本次会话的 scratchpad 目录。**该目录随会话结束可能被清理，如需长期保留请及时复制。**

## 审批结论

`CHANGES REQUIRED`

理由：M-11 已解决，但遗留事项 R-01 使交付包内 `FINAL_QA.md` 当前记录为 `FAIL · 19/20`。在渲染产物补齐并重跑 `final_qa.py` 恢复 20/20 之前，不得批准。此外第二轮的 M-09、M-10、m-13 状态本轮未复核，仍为「未解决」。
