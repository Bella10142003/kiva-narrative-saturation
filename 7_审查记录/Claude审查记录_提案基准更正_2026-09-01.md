# Claude 独立审批记录 · 提案基准更正（第四轮）

> 续接 `Claude审查记录.md`（08-15，提案阶段）、`Claude审查记录_最终交付包_2026-08-30.md`（第二轮）、`Claude审查记录_固定效应口径_2026-09-01.md`（第三轮）。
> 编号续接：Blocker 本轮首次出现，起编 B-01；Major 续 M-12；Minor 续 m-14。

## 审查信息

| 项目 | 内容 |
| --- | --- |
| 审查日期 | 2026-09-01 |
| 审查对象 | `5_最终交付包/MA-Hackathon-Final-2026-09-04/` 全部产物，对照**用户于本轮提供并确认的最终提案 PDF**《WHEN EVERY STORY SOUNDS THE SAME》 |
| **本轮起因** | 用户询问 07 的控制变量与 RQ 覆盖，核查中发现**此前全部审查所依据的提案文档有误** |
| **审查范围（重要限制）** | 本轮为**基准更正 + 定向复核**。已复核：提案 RQ1–RQ4 与实现的逐项对应、`fit_main_models.py` 全部设定、四张结果表、`holdout_validation.csv`、`MANIFEST_SHA256.txt`、`frozen_spec.csv`、`stage4b_model_manifest.json`。**未复核**：REQ-CON-001～010、REQ-DATA-001～002、SCORE-001～005、图表、PPTX、HTML 报告、第二轮遗留的 M-09／M-10／m-13。**不得将本文件中未出现的项目理解为「已核查且通过」。** |
| 是否修改起草文件 | **否。** 未修改任何 `2_提案/`、`3_分析步骤/`、`5_最终交付包/` 文件 |
| 执行方式 | 前台单智能体；未启动 Workflow、subagent、多智能体 |

### 本轮实际执行的独立计算

均在只读方向进行，未写入项目目录：

1. 以 `4_中间产物/我算出来的结果/data/model_data.parquet` 独立重跑主模型，**六个系数逐位复现交付包当前值**（C=0.5131087364214306 等），确认当前血缘可复现。
2. 补跑提案未要求但用于诊断的嵌套序列 M0（仅控制变量+FE）→ M1（+H）→ M2（+C）→ M3（交付包主模型）。
3. 执行 PDF 指定但交付物缺失的 **RQ2 联合检验**（V、G、V×G 同时为零）：Wald χ² = 10.0549，**p = 0.006556**。
4. 重跑「删去 V/G/V×G 通道」的主模型，量化滞后通道对当前通道系数的影响。
5. 核对 `MANIFEST_SHA256.txt` 与实际文件 SHA-256。

---

## Blocker

| 编号 | 位置 | 对应标准 | 原因与影响 | 修改建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| **B-01** | `2_提案/` 全部 5 份文档、`CONTEST_RULES.md` §3.1 与 §9、`README.md` 假设对照表，以及**第一至第三轮全部审查记录** | CLAUDE.md「审批前检查」第 3 条（对照竞赛要求与评分标准逐项检查）；CONTEST_RULES 规则 2（可追溯性） | 实际提交的提案是《WHEN EVERY STORY SOUNDS THE SAME — Separating Market Crowding, Active Narrative Competition, and Narrative Wear-Out on Kiva》，结构为 **RQ1–RQ4**。但 `2_提案/提案正文.md` 是另一份草稿《Beyond Crowding》，结构为 **H1–H4**，团队栏仍为 `[TO CONFIRM]`，数据描述仍为「100 funded loans」。两份提案在**研究问题、核心估计量、次级假设、主文本字段**四个维度上均不同：<br>· 提交版 RQ1 测 **C×H 交互**（"neither alone is sufficient"）；草稿版 H1/H2 测 S_i 主效应与嵌套<br>· 提交版 RQ2 = **V/G/V×G 滞后「wear-out」通道**；草稿版**完全没有这条通道**<br>· 提交版无 H3；草稿版 H3 需 200 条盲编码 + F1≥0.80 门槛（`假设清单.md` ASM-016）<br>· 提交版主文本字段看诊断后定（实际选 `use`）；草稿版写死 "primary text is *description* only"（ASM-005）<br><br>**影响**：`合规对照矩阵.md`、`假设清单.md`、`证据来源.md`（EVD-011）、`CONTEST_RULES.md` §3.1 全部以草稿为基准建立，**无法用于审批本交付物**。更严重的是，第三轮 M-11 的处置方向因此错误（见 M-12）。 | ①以 PDF 为唯一基准重建 `合规对照矩阵.md` 的研究范围部分、`假设清单.md`（ASM-004/005/006/007/016 需重写或作废）、`CONTEST_RULES.md` §3.1 与 §9 的人工确认记录；②`README.md` 的 H1–H4 对照表改为 RQ1–RQ4；③明确 `2_提案/提案正文.md` 的地位（作废或标注为未提交草稿），避免后续再次误用 | **未解决** |
| **B-02** | `MANIFEST_SHA256.txt`（mtime 2026-08-28）对照 `outputs/` 下实际文件（mtime 2026-09-01 14:40） | CONTEST_RULES 规则 2（可追溯性）；交付包自带的完整性承诺 | 第三轮的全链路重跑更新了 `outputs/` 全部结果表，但 **`MANIFEST_SHA256.txt` 未随之重新生成**，仍停留在 08-28 血缘。实测三个文件全部不符：<br><br>`rq1_main_table.csv` 清单 `f3fcb837…`／3306 B，实际 `0f27e112…`／3308 B<br>`scenario_contrasts.csv` 清单 `1e4d135f…`，实际 `02b490eb…`<br>`robustness.csv` 清单 `7066334b…`，实际 `523f224a…`<br><br>清单记录的 3306 B 对应 `_rebuild/_verify/` 的 08-28 版本。**交付包自带的完整性校验当前处于失效状态**，评委若执行校验会直接得到不匹配。此外 `audit/stage4a_model_data_manifest.json` 记录的 `model_data_path` 指向 `_rebuild/`，其 `bytes=188132614` 与 `4_中间产物` 下的 187961314 亦不一致（该项属第三轮已记录的 R-02，非本条） | 在最终打包前重新执行 `build_final_zip`（或等价的清单生成步骤）重建 `MANIFEST_SHA256.txt`，并复核 ZIP 内容与清单一致 | **未解决** |

---

## Major

| 编号 | 位置 | 对应标准 | 原因与影响 | 修改建议 | 状态 |
| --- | --- | --- | --- | --- | --- |
| **M-12** | `src/fit_main_models.py` 的 `CORE_CONTROLS`（4 项）与 `FIXED_EFFECTS`（3 项） | 提案 PDF p.4：*"controlling for loan, borrower, repayment and **posting-time characteristics**, platform-wide volume, and country, activity and week fixed effects"*；SCORE-002（严谨性） | 第三轮 M-11 将固定效应由七重改回三重，使代码与 `frozen_spec.csv` 对齐。**但该处置是以草稿提案为基准做出的，与实际提交的 PDF 冲突。** 改动后 `gender`、`repaymentInterval`、`posting_dow`、`posting_hour` **全部退出模型**——它们在 `fit_main_models.py:253-261` 仍被读入、在 287-290 仍被转 category，但既不在 `CORE_CONTROLS` 也不在 `FIXED_EFFECTS`，成为死列。<br><br>后果是 **PDF 明确承诺的 "posting-time characteristics" 现在完全不受控**。`week_id` 固定效应只吸收周级别的共同冲击，不吸收周内的星期几与小时效应；而 Kiva 的上架时点与出借人在线分布高度相关，这是可预期的混淆来源。`repayment characteristics` 亦只剩 `log_repayment_term`，`repaymentInterval` 已不在模型内。<br><br>**方向判断**：第三轮已验证七重→三重使四表 44 项符号翻转 0、显著性翻转 0，因此这不是结论层面的风险，而是**与提交承诺不符**的风险——评委手持 PDF 对照 `METHODS_APPENDIX.md` 即可发现承诺的控制变量缺失 | **⛔ 本栏原建议已作废，勿照此执行。** 原建议以「恢复七重固定效应」为首选方案，会重新违反 `frozen_spec.csv` 与 PDF 各自的 FE 承诺（PDF 原文的第三个并列成分正是「**country, activity and week** fixed effects」，恰为三重），并需反向修改冻结文档。<br><br>**实际采用并已完成的方案**：`FIXED_EFFECTS` **保持三重不变**，把 `gender`／`repaymentInterval`／`posting_dow`／`posting_hour` 四项加入 `CORE_CONTROLS`（由 4 项增至 8 项）。PDF 那句话本身是三个并列成分——「loan, borrower, repayment and posting-time characteristics」「platform-wide volume」「country, activity and week fixed effects」——前两组是**控制变量**、第三组才是**固定效应**。按此归位后，PDF 承诺与 `frozen_spec.csv` **同时成立，无需修改任何冻结文档**。<br><br>该处置对估计值是中性的：低基数类别变量放在 FE 块（被吸收）或控制块（被估计），在设计矩阵中是同一组哑变量，由 Frisch–Waugh–Lovell 定理其余系数不变。实测同一 parquet 血缘下七重 FE 与三重+4控制的 β(C) 前 12 位小数一致；唯一变化的是 within-R²（0.3729→0.3868），因扣除的固定效应减少，属记账口径变化。详见处置记录第一至四节 | **已解决**（2026-09-01） |
| **M-13** | `outputs/scenario_contrasts.csv` recent 通道行；`outputs/robustness.csv`；`README.md:103` | 提案 PDF p.4：*"**RQ2 jointly tests V, G and V×G**"* | PDF 为 RQ2 指定的检验是 V、G、V×G 的**联合检验**。交付物中不存在该检验，只有：①三个单项系数；②一个联合 P25→P75 情景对比（−0.955%，CI 跨零）。<br><br>两者结论方向相反。本轮执行 PDF 指定的联合 Wald 检验：**χ² = 10.0549，p = 0.006556，显著**。而 `README.md:103` 将情景对比写作「对照（recent 通道）−1.4%［−15.8, +15.4］，区间跨 0」，把 RQ2 的**研究对象**当成失败的安慰剂来呈现。<br><br>**这使 RQ2 由一个成立的正面发现被误述为空结果。** 单项证据同样支持 RQ2：V×G = 0.0465（p = 0.0029）；PDF 进一步要求「test whether it **survives** removing repeated institutional boilerplate」，`active_residual_text` 规格下 V×G = 0.0611（p = 0.0005），**更强，存活** | ①在 `fit_main_models.py` 中补入 V/G/V×G 的联合检验并写入 `rq2_raw_vs_residual.csv`；②改写 `README.md:103` 与报告中 recent 通道的表述，明确它是 RQ2 的研究对象而非对照组；③保留情景对比但注明其跨零源于 βV 与 βG 反号抵消，不等于通道无关联（另见 M-18） | **未解决** |
| **M-14** | `src/fit_main_models.py`（`boilerplate_share` 零出现）；对照 `src/prepare_model_data.py:120` 已产出该列 | 提案 PDF p.4：*"we estimated each headline model on both the raw and the residual text before and after template removal, **while retaining boilerplate share as a new variable**"* | PDF 明确承诺 headline model 保留 boilerplate share 作为变量。该列在 05／06 已算出并写入 `model_data.parquet`，但 `fit_main_models.py` 的读取列表中没有它，任何 headline model 都未纳入。它仅出现在 `predict_engine.py`（Narrative Attention Engine 的特征集），而 Engine 不是 headline model。<br><br>**影响**：PDF 用 raw／residual 对比来「distinguishing between institutional repetition and substantive narrative overlap」，boilerplate share 是这一区分的第三条腿。缺失它，`active_residual_text` 规格 C×H 由 0.1045 降至 0.0524（p 由 0.0023 升至 0.132）这一现象无法判断是模板效应还是测量衰减 | 将 `boilerplate_share` 加入 `CORE_CONTROLS` 并重跑 headline model；或在报告中明示该承诺未执行及其理由 | **未解决** |
| **M-15** | 全部 `outputs/`（无跨行业／全平台池变体） | 提案 PDF p.2 RQ4：*"…and does the relevant attention market operate mainly **within sectors or across the whole platform**?"* | RQ4 是双问题：①各行业／国家的强弱分布；②注意力市场究竟运行在行业层面还是全平台层面。交付物有 `rq4_sector_effects.csv`、`rq4_country_effects.csv` 及对应 global tests，**覆盖①；②无任何产出**。全项目检索无 `platform_pool`／`cross_sector`／全平台池变体，`log_platform_posting_7d` 是控制变量而非池定义 | 构造一个不按 sector 分组的全平台池 C／H 变体，与现有同行业池规格并列比较（可复用 05 的池构造代码）；若时间不允许，须在报告与演讲稿中明确声明 RQ4 后半问未作答 | **未解决** |
| **M-16** | `REPORT.md`、`METHODS_APPENDIX.md`、`SPEAKER_NOTES_EN.md`、`JUDGE_QA_BILINGUAL.md` | SCORE-005（Clarity, structure, and communication quality，10%）；CONTEST_RULES §4「必答内容」 | 检索 `RQ1`／`RQ2`／`RQ3`／`RQ4`，四份文档**零命中**。最终交付物从头到尾没有把结果映射回提交提案的四个研究问题。`REPORT.md:42` 引用了 `outputs/rq1_main_table.csv` 这个文件名，但正文从未说明它回答的是 RQ1。<br><br>**影响**：评委手持提案 PDF 审阅交付物时，无法直接建立「提案承诺 → 交付结论」的对应关系，直接影响 SCORE-005，并间接影响 SCORE-001／002 的可读性 | 在 `REPORT.md` 增设一节，按 RQ1／RQ2／RQ3／RQ4 逐条给出结论、支撑表格与限制；演讲稿同步 | **未解决** |
| **M-17** | `outputs/holdout_validation.csv` | CONTEST_RULES 规则 5（不得把预期结果写成已完成发现）与规则 2；SCORE-002 | 2025 样本外对比「仅控制变量」与「控制变量+叙事特征」，**七项指标中六项变差**：<br>R² 0.5524→0.5370；RMSE 1.2253→1.2462；MAE 0.9646→0.9781；ROC AUC 0.8986→0.8921；Brier 0.1354→0.1369；准确率 0.8166→0.8050；仅 log loss 0.4312→0.4300 改善。<br><br>该表在交付包内，但报告未就「叙事特征在样本外不提升预测力」作任何陈述。需注意此处用的是 `predict_engine.py` 的 SGD 模型而非 HDFE，两者不可直接互证——但 `README.md:78` 将 09 描述为「样本外：叙事特征到底有没有增量预测力」，按其自身口径答案是否定的 | 在报告中如实披露该结果，并说明它与 HDFE 的条件关联结论并不矛盾（预测力与条件关联是两回事）；同时修正 `README.md:78` 的表述 | **未解决** |
| **M-18** | `2_提案/假设清单.md` ASM-017；`outputs/scenario_contrasts.csv` recent 通道；`README.md:103` | **CONTEST_RULES 规则 12**（不得把「不显著」直接解释为无关联；未确定最小具管理意义关联阈值时，结论须保持 inconclusive） | ASM-017（「managerially meaningful 正关联的最小量级」）状态仍为**待确认**，规则 12 明确该项未确认时不得把跨零区间解释为无关联。当前 `README.md:103` 与报告对 recent 通道的呈现方式，实质上就是把跨零区间当作「无效应」来用（且如 M-13 所述，这恰好是 RQ2 的研究对象，联合检验显著）。<br><br>本条与 M-13 是同一现象的两个层面：M-13 是「用错了检验」，本条是「用跨零区间下无关联结论」违反强制规则 | ①人工确定 ASM-017 阈值，或②将 recent 通道的表述改为 inconclusive／改用 M-13 的联合检验结论。二者择一，不得维持现状 | **未解决** |

---

## Minor

| 编号 | 位置 | 对应标准 | 原因 | 修改建议 |
| --- | --- | --- | --- | --- |
| m-14 | `src/fit_main_models.py` 的 `exclude_refunded` 模型；`outputs/robustness.csv` | SCORE-002 | 该稳健性检验与主模型**逐位相同**（C×H 均为 0.10449150348203909，n 均为 1234131）。实测训练样本中 `status='refunded'` 计数为 **0**（1,637 笔全部在更早的筛选中被滤除），故过滤器未删除任何行。读者会将「与主模型一致」误读为「结论对排除退款贷款稳健」 | 删除该行，或在表中注明 n_refunded_removed = 0 |
| m-15 | `outputs/robustness.csv`（各规格仅存 CH 与 VG 两项） | 提案 PDF「RQ2 jointly tests V, G and V×G」 | `fit_main_models.py` 的 `run()` 对每个模型只保留 CH 与 VG。RQ2 要求 V、G、V×G 三项，稳健性表缺 V 与 G；RQ1 的头条情景中 βC 贡献 76%、βH 贡献 27%，二者在替代池规格下亦从未复核 | 将 C、H、V、G 四项一并写入 `robustness.csv` |
| m-16 | `src/fit_main_models.py:253-261, 287-290` | 代码卫生 | `gender`、`repaymentInterval`、`posting_dow`、`posting_hour` 仍被读取并转 category，但不进入任何模型（M-12 的副产物），属死列，徒增内存与误导性 | **已随 M-12 解决（2026-09-01）**：四项已加入 `CORE_CONTROLS`，由死列变为真实控制变量，读取与 category 转换现在都是必要的，无需移除 |
| m-17 | `CLAUDE.md`「审批前检查」第 3、4 条与「角色与权限」 | — | 其中的路径 `proposal/proposal.md`、`proposal/requirements.md`、`proposal/compliance-matrix.md`、`proposal/evidence.md`、`proposal/assumptions.md`、`reviews/claude-review.md` **在本项目中均不存在**，实际对应 `2_提案/提案正文.md`、`竞赛要求清单.md`、`合规对照矩阵.md`、`证据来源.md`、`假设清单.md` 与 `7_审查记录/`。本轮按项目既有约定写入 `7_审查记录/` | 更新 `CLAUDE.md` 的路径以匹配实际目录结构 |

---

## 上一轮遗留事项复核

| 编号 | 内容 | 本轮状态 |
| --- | --- | --- |
| R-01 | `final_qa.py` 的 PPTX 渲染门 FAIL，19/20 | **仍未解决。** 实测 `FINAL_QA.md` 当前为 `**FAIL · 19/20 deterministic checks passed.**`，失败项仍是「PPTX has 16 slides… renders 16 pages」 |
| R-02 | 流水线非逐位可复现（三份 `model_data.parquet` 字节数各异，系数第 5 位小数漂移） | **性质已澄清，不再是问题。** 本轮以 `4_中间产物` 的 parquet 独立重跑，**六个系数逐位复现交付包当前值**。R-02 描述的漂移发生在不同构建血缘之间；同一血缘内可复现。`_control/` 保留 7-FE 旧值（C=0.5203）系第三轮有意保留的「改动前」快照，非错误 |
| R-03 | `Kiva_Final_Report_2026-09-04.html` 系手工文本替换而非工具重生成 | 本轮未复核 |
| M-09／M-10／m-13 | 第二轮遗留 | 本轮未复核，仍为「未解决」 |

---

## 按提交提案 RQ1–RQ4 的覆盖情况

| RQ | 提案指定的检验 | 交付物状态 |
| --- | --- | --- |
| **RQ1** Active narrative saturation | "RQ1 tests C×H" | **✓ 已作答。** β(C×H) = 0.1045，SE 0.0323，p = 0.0023。注：头条 +123.51% 是 C、H、C×H 的联合 P25→P75 情景，其中 βC 贡献约 76%、βH 约 27%、βC×H 约 −3%（该情景区间内 ΔC×H 为负），与 RQ1「交互」的表述存在张力，建议在报告中说明 |
| **RQ2** Competition versus wear-out | "RQ2 jointly tests V, G and V×G"；并检验剥离机构模板后是否存活 | **△ 结论成立但呈现错误。** 联合检验交付物中缺失（本轮补算 χ²=10.05, p=0.0066，显著）；boilerplate 存活性成立（residual 规格 V×G=0.0611, p=0.0005）。见 M-13、M-18 |
| **RQ3** Evolution of the two channels | 与 2016–2024 年份指示变量交互，报年度边际效应 | 本轮**未复核**（产物在 `rq3_year_effects.csv`，由 08 生成） |
| **RQ4** Segment boundaries | ①行业与国家分布；②行业内 vs 全平台 | **① 有产出，② 无产出。** 见 M-15 |

---

---

## 处置记录（2026-09-01，用户明确授权「全链路对齐，保留有用的内容」）

用户就两个岔路作出选择：**M-12 采用「四项改为控制变量并全链路重跑」**；**副本清理采用「删 `_verify` 和空目录，保留 `_control` 与 `_rebuild`」**。

### 一、修改前的验证（隔离环境）

先在只读方向验证「四项作为控制变量」与「四项作为固定效应」的代数等价性：以 `4_中间产物` 的 parquet 独立拟合，六个系数与 `_control`（七重 FE）的差异在 1e-5 量级（即 R-02 记录的 parquet 血缘漂移），**总 R² 0.562088 对 0.5620872 逐位一致**。确认等价后才动项目文件。

完整备份（交付包 + `3_分析步骤` + 中间产物 outputs，9.8 MB）已存入本次会话 scratchpad。**该目录随会话结束可能被清理。**

### 二、代码修改

| 文件 | 修改内容 |
| --- | --- |
| `src/fit_main_models.py` | `CORE_CONTROLS` 由 4 项增至 8 项，补入 `gender`／`repaymentInterval`／`posting_dow`／`posting_hour`，并附注提案 p.4 原文；`columns` 增加 `dict.fromkeys` 去重 |
| `src/fit_heterogeneity.py` | `columns` 去重（避免与新增控制项重复） |
| `src/description_robustness.py` | `model_columns` 去重 |
| `src/final_qa.py` | 头条回归锁 123.5064→**124.3508**、−14.5695→**−14.6818**，并按代码既有体例补注变更理由 |
| `3_分析步骤/07_主模型HDFE.ipynb` | cell 1 内嵌的 `CORE_CONTROLS` 同步为 8 项 + 去重行 |

`FIXED_EFFECTS` **保持三重不变**——这正是本方案的价值：PDF 的「posting-time characteristics」承诺与 `frozen_spec.csv` 的「Country + Activity + Week FE」承诺**同时成立**，无需反向修改任何冻结文档。`fit_heterogeneity.py`(08) 与 `description_robustness.py`(10) 通过 `from fit_main_models import CORE_CONTROLS` 自动跟随。`predict_engine.py` 只 import `add_standardized_columns, load_scalers`，不受影响，未重跑（与第三轮同理，避免无谓浮点漂移）。

### 三、重跑链路

在 `_rebuild/MA-Hackathon-Final-2026-09-04/`（带 `data/` 的构建目录）按 RUNBOOK 顺序执行 `fit_main_models` → `fit_heterogeneity` → `description_robustness` → `check_joint_support` → `make_figures` → `create_and_execute_notebooks` → `build_deliverables`，随后提升至交付包并执行 `final_qa` 与 `build_final_zip`。`4_中间产物/我算出来的结果/` 亦同步重跑。

**确定性门全部通过**：`joint_support_pass=true`、六个审计 notebook 执行成功、ZIP 校验 PASS（148 成员）。

### 四、结果变动

| 项 | 三重 FE（改前） | 控制变量版（改后） | 说明 |
| --- | ---: | ---: | --- |
| 头条 C&H P25→P75 | +123.506% | **+124.351%** | 恢复至 M-11 改动前的值 |
| ↳ 95% CI | [100.867, 148.697] | **[103.711, 147.082]** | |
| 72 小时快速筹款率 | −14.569pp | **−14.682pp** | |
| β(C) | 0.5131 | **0.5202** | |
| β(H) | 0.4137 | **0.4036** | |
| β(C×H) | 0.1045 | **0.1017** | RQ1，p=0.0033 |
| β(V×G) | 0.0465 | **0.0417** | RQ2，p=0.0037 |
| β(V×G) residual | 0.0584 | **0.0585** | RQ2 存活性，p=0.0007 |

**四表符号翻转 0，显著性翻转 0。**

### 五、文档同步

`REPORT.md`、`METHODS_APPENDIX.md`、`RUNBOOK.md`、`DELIVERY_MAP.md`、`report_artifact.json` 由 `build_deliverables.py` 重新生成。以下四份不在生成范围内，按第三轮的结构锚定方式手工更新（仅在 `portable-source-value-text`、`Source for …`、`<text>`、`portable-table-number`、`<strong>` 五类文本节点内替换）：

| 文档 | 修改处数 |
| --- | --- |
| `JUDGE_QA_BILINGUAL.md` | 3 |
| `SPEAKER_NOTES_EN.md` | 6 |
| `SPEAKER_CUES_ZH.md` | 4 |
| `Kiva_Final_Report_2026-09-04.html` | 12 个数值合计 **54 处** |

HTML 的新旧值全部等长，替换后**字节数（648,329）与标签数（3,943）均未变化**；SVG `<path d="M90.65,74.9064…">` 中的 4 处几何坐标未被误伤（`0.65` 共 7 处，仅替换节点内的 3 处）。六份文档现均只含新值，旧值零残留。

### 六、B-02 的真实成因

`build_final_zip.py:258` 通过 `archive.writestr()` 把 `MANIFEST_SHA256.txt` **只写进 ZIP，从不落盘**。磁盘上那份是 08-28 遗留的孤儿文件，任何重跑都不会更新它——这才是 58 处不符的原因，不是产物本身有问题。

核实：**ZIP 内清单 145 条、0 处不符**。已将 ZIP 内的权威清单写回磁盘副本。磁盘树与之仍有 11 处差异，经逐字节比对确认**纯为路径脱敏**（`"root": "<HOME>/…"` → `"root": "<FINAL_ROOT>"`），属打包时的正常行为。`final_qa.py` 不校验该文件。

### 七、副本清理

| 对象 | 处置 | 理由 |
| --- | --- | --- |
| `_rebuild/_verify/` | **已删除** | `src` 已是三重而 `outputs` 停在 08-28 七重血缘（C=0.5202465），内部不自洽；本次核查中一度被误读为有效血缘 |
| `_control/MA-Hackathon-Final-2026-09-04 2` | **已删除** | 0 字节空目录 |
| `_control/` | 保留 | 七重 FE 时期的历史快照，有审计价值 |
| `_rebuild/` | 保留 | 含 347 MB `data/model_data.parquet`，是重跑工作区 |
| `_rebuild/MA-Hackathon-Final-2026-09-04.zip` | **已删除**（用户指示） | 08-31 17:31 的旧包，与当前交付包不同源。权威 ZIP 是 `5_最终交付包/MA-Hackathon-Final-2026-09-04.zip` |

### 七之二、归档后新发现（2026-09-01）

用户归档提案 PDF 后核验发现：**`2_提案/提案定稿.docx`（08-15）名为「定稿」，内容却是未提交的草稿《Beyond Crowding》**——团队栏 `[TO CONFIRM]`，全文无 `RQ1`–`RQ4`。这与 B-01 是同一类命名陷阱：一个看似权威的文件名指向非基准内容。已登记为 EVD-013。**已于同日按用户指示归档并重命名**（见第七之三节）。

### 七之三、草稿归档（2026-09-01，用户指示「执行」）

核验确认 `提案大纲.md` 是 Beyond Crowding 的配套大纲——SEC-06「Primary models for **H1 and H2**」、SEC-07「Secondary analysis for **H3**」、SEC-08「Exploratory analysis for **H4**」，使用 S_i／N_i 记号，**wear-out／V×G 零命中**（即提交版 RQ2 的全部内容它一句没有）。因此它**不是提交版的大纲，无需按 RQ 重建**，应整体归档。

三份草稿文件移入 `2_提案/_未提交草稿/` 并重命名：

| 原路径 | 新路径 |
| --- | --- |
| `2_提案/提案正文.md` | `2_提案/_未提交草稿/提案正文_BeyondCrowding_未提交.md` |
| `2_提案/提案定稿.docx` | `2_提案/_未提交草稿/提案定稿_BeyondCrowding_未提交.docx` |
| `2_提案/提案大纲.md` | `2_提案/_未提交草稿/提案大纲_BeyondCrowding_未提交.md` |

新增 `2_提案/_未提交草稿/README.md`，说明目录性质、两份提案的四维度差异，以及**保留而不删除的四条理由**（审计链依赖 EVD-011 与 ASM-004/005/006/007/016；CONTEST_RULES 规则 2 与规则 3；它是前三轮审查错误链的源头，需可复核；H3 盲编码协议与嵌套 H1→H2 设计有独立方法论价值）。

引用路径同步更新 19 处，分布于 `README.md`(3)、`CONTEST_RULES.md`(4)、`假设清单.md`(2)、`竞赛要求清单.md`(3)、`合规对照矩阵.md`(3)、`证据来源.md`(1)，以及两份移动文件自身的相对链接(7)。

**顺带修正两处更严重的问题**：`README.md` 开篇原文是「照着[提案正文]…把分析做出来」，链接指向**草稿**——已改为指向提案 PDF；`CONTEST_RULES.md` §8 原本要求「提案大纲需按 RQ1–RQ4 重建」，已按上述核验结论改为无需重建。

历史审查记录（`Claude审查记录_最终交付包_2026-08-30.md` 中对 `提案正文.md:52` 的行引用）**未作修改**——它是当时状态的陈述，回溯改写会损害审计记录的可信度。

`5_最终交付包/` 由 383 MB 降至 383 MB（删除项体积很小），剩余空目录 0 个。

> ⚠️ `_rebuild/MA-Hackathon-Final-2026-09-04.zip`（08-31 17:31）是旧包，与当前交付包不同源。权威 ZIP 是 `5_最终交付包/MA-Hackathon-Final-2026-09-04.zip`。建议删除前者以免再次混淆。

### 八、基准文档更正（B-01 的可事实确认部分）

| 文件 | 处置 |
| --- | --- |
| `README.md` | H1–H4 对照表重写为 **RQ1–RQ4**（含各 RQ 的提案原文与指定检验）；变量表中 `recent` 通道由「滞后对照队列」改为「**RQ2 的检验对象（wear-out），不是对照组／安慰剂**」；关键数字更新并标注 CONTEST_RULES 规则 12；`最终 QA` 由 `20/20` 改为 **19/20**；步骤表 07 补上 8 个控制变量、09 补上样本外结论为否 |
| `CONTEST_RULES.md` | §3.1 由 H1–H4 重写为 RQ1–RQ4；§8 增列 PDF 未归档与 `2_提案/` 待重建；§9 增加两条变更记录并将 08-15 那条的研究优先级部分标为已作废 |
| `2_提案/提案正文.md` | 顶部加「⛔ 未提交的早期草稿 — 不是基准」声明与四维度对照表 |
| `2_提案/假设清单.md` | 顶部加基准更正表，逐条标注 ASM-004/005/006/007/012/016 的作废或重写要求，并指明 **ASM-017 仍然有效且仍在阻塞** |
| `2_提案/证据来源.md` | EVD-011 的研究优先级部分标为已被取代；新增 **EVD-012** 登记提案 PDF（含团队 5 人姓名学号、模型方程、RQ1/RQ2 指定检验及页码定位） |
| `2_提案/合规对照矩阵.md`（31 处 H）<br>`2_提案/竞赛要求清单.md`（14 处） | 各加「⛔ 本文件不可用于审批」声明。**仍需按 RQ 重建** |
| `2_提案/提案大纲.md`（60 处 H） | 先加声明，后经核验确认是草稿配套大纲，**已整体归档，无需重建**（见第七之三节） |

**未做的部分**：上述三份文件合计 105 处 `H1`–`H4` 引用的**实质重建未执行**。这些是团队自身的合规论证与要求映射，代写等同于制造合规记录，违反 CONTEST_RULES 规则 1（禁止虚构）。需团队按 PDF 自行重建。

---

## 问题状态更新

| 编号 | 更新后状态 |
| --- | --- |
| **B-01** | **部分解决**。可事实确认的部分已更正（见处置记录第八节）。**提案 PDF 已归档**：`2_提案/5 GUYS 提案.pdf`（6 页，602,324 字节，SHA-256 `682480268af5db3de10a5dd661ea743e569b1d4b9a3c674c9241b478a6afcce4`，文件时间 08-24 16:18，在截止前），关键原文已逐字核验并登记为 EVD-012。**草稿已隔离归档**至 `2_提案/_未提交草稿/`（见第七之三节），`提案大纲.md` 经核验为草稿配套、无需重建。**剩余工作量由 105 处降至 45 处**：`合规对照矩阵.md`（31 处，40 条 CMP 中仅 10 条涉及）与 `竞赛要求清单.md`（14 处，24 条 REQ 中仅 1 条涉及）待团队按 RQ1–RQ4 局部改写；两份文件的官网要求映射部分不受影响 |
| **B-02** | **已解决**。ZIP 内清单 145/145 自洽，磁盘副本已刷新 |
| **M-12** | **已解决**。四项恢复为控制变量，PDF 承诺与冻结规范同时成立，全链路重跑完成 |
| **M-13** | **未解决**。RQ2 的 V/G/V×G 联合检验仍未写入产物（本轮补算 χ²=10.05、p=0.0066 仅存在于审查记录）。`README.md` 的定位表述已更正 |
| **M-14** | **未解决**。`boilerplate_share` 仍未进入 headline model |
| **M-15** | **未解决**。RQ4 后半问仍无产出，已在 `README.md` 标注为已知缺口 |
| **M-16** | **未解决**。`REPORT.md` 等交付文档仍零处提及 RQ1–RQ4 |
| **M-17** | **降级为已解决**。复核发现 `REPORT.md:15` 与 `:66-68` 本就完整披露了样本外劣化（含 AUC 0.8921 对 0.8986、"解释性信号没有转化为稳定增量预测"）。缺陷仅在 `README.md:84` 的表述，已修正。**此前定级为 Major 系我复核不足** |
| **M-18** | **未解决**。ASM-017 阈值仍待人工确定；`README.md` 已标注规则 12 |
| m-14 | 未解决（`exclude_refunded` 空检验） |
| m-15 | 未解决（`robustness.csv` 仍只报 CH／VG） |
| **m-16** | **已解决**。四列由死列变为真实控制变量 |
| m-17 | 未解决（`CLAUDE.md` 路径失效，需用户修改） |
| R-01 | **未解决且本环境无法解决**。`node not found`，PPTX 无法渲染，`FINAL_QA.md` 维持 `FAIL · 19/20` |
| R-02 | 性质已澄清：同一血缘内可逐位复现，跨血缘漂移在 1e-5 量级 |
| M-09／M-10／m-13 | 第二轮遗留，本轮仍未复核 |

---

## 审批结论

`CHANGES REQUIRED`

理由：B-01 仅部分解决（`2_提案/` 三份文件共 105 处引用待团队按 PDF 重建，提案 PDF 未归档），M-13、M-14、M-15、M-16、M-18 五项 Major 未解决，R-01 使交付包内 `FINAL_QA.md` 维持 `FAIL · 19/20`，第二轮遗留的 M-09／M-10／m-13 未复核。

按 CLAUDE.md「审批规则」，存在任何未解决的 Blocker 或 Major 时不得批准。

**剩余处置顺序**：M-13（把 RQ2 联合检验写进 `fit_main_models.py` 与 `rq2_raw_vs_residual.csv`）→ M-16（`REPORT.md` 增设 RQ1–RQ4 结论节）→ M-14（`boilerplate_share` 入模）→ M-15（全平台池变体，或明确声明未作答）→ M-18（人工确定 ASM-017 阈值）→ B-01 剩余部分 → R-01（需有 node 的环境）。

最终提交仍须人工确认。
